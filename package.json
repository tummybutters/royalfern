{
  "name": "royal-fern",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check:source": "node scripts/check-source.mjs",
    "check:links": "node scripts/check-links.mjs",
    "check:budgets": "node scripts/check-budgets.mjs",
    "check:dist": "npm run check:links && npm run check:budgets",
    "check": "npm run check:source && npm run build && npm run check:dist"
  },
  "devDependencies": {
    "vite": "^7.1.0"
  }
}
